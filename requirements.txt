# What this notebook imports. Nothing here is the renderer: Launchpad installs
# nbconvert and ipykernel itself, because the engine is the platform's business
# and not the report's.
pandas>=2.0
matplotlib>=3.8
